# 安全密钥（用于 IP 哈希，请务必修改）
SECRET_KEY=please-change-me-to-a-random-string

# 管理员 Token（用于重新加载数据）
ADMIN_TOKEN=your-admin-token-here

# 允许的跨域来源（默认 * 表示允许所有，生产环境建议指定具体域名）
ALLOWED_ORIGINS=*

# 数据库配置（支持 SQLite 和 PostgreSQL）
# SQLite: DB_URL=sqlite:///./storage/app.db
# PostgreSQL: DB_URL=postgresql+psycopg2://user:pass@host:5432/dbname
DB_URL=postgresql+psycopg2://graveyard_user:graveyard_pass@postgres:5432/graveyard

# PostgreSQL 配置（仅用于 Docker Compose）
POSTGRES_DB=graveyard
POSTGRES_USER=graveyard_user
POSTGRES_PASSWORD=graveyard_pass

# 可选：hCaptcha 配置（留空则不启用）
HCAPTCHA_SECRET=
HCAPTCHA_SITEKEY=
